PORT=5000
NODE_ENV=development
JWT_SECRET=stackit_super_secret_jwt_key_2026
JWT_EXPIRES_IN=7d

# Database Configuration (Optional PostgreSQL - defaults to SQLite fallback if PG not reachable)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=stackit_db
DB_USER=postgres
DB_PASSWORD=postgres
DATABASE_URL=postgres://postgres:postgres@localhost:5432/stackit_db
